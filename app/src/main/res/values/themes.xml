<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- the theme applied to the application or activity -->
    <style name="TipcoinTheme"
        parent="@style/Theme.AppCompat.Light.DarkActionBar">
        <item name="android:actionBarStyle">@style/TipcoinActionBar</item>

        <!-- Support library compatibility -->
        <item name="actionBarStyle">@style/TipcoinActionBar</item>
    </style>

    <!-- ActionBar styles -->
    <style name="TipcoinActionBar"
        parent="@style/Widget.AppCompat.Light.ActionBar.Solid.Inverse">
        <item name="android:background">@drawable/background</item>

        <!-- Support library compatibility -->
        <item name="background">@drawable/background</item>
    </style>
</resources>